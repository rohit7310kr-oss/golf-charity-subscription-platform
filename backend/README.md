# Golf Charity Backend

Backend API for the golf charity subscription MERN application.

## Setup

1. Install dependencies: `npm install`
2. Create `.env` file from `.env.example`
3. Run development server: `npm run dev`
